export default class Utils {
  constructor(client) {
    if (!client) {
      throw TypeError("The Client Class Is Not Defined, Please Use It As So For ESM, \"this.utils = new Utils(this)\"")
    }
  }
  // unfinished shit here
}
